[package]
name = "osmium-kernel"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "osmium-kernel"
test = false
bench = false

[dependencies]
bootloader_api = "0.11"
spin = "0.9"

[dependencies.noto-sans-mono-bitmap]
version = "0.2.0"
default-features = false
features = [
    "regular",
    "size_16",
    "unicode-basic-latin",
    # required for the fallback char '�'
    "unicode-specials",
]
